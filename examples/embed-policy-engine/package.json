{
  "name": "@osas-examples/embed-policy-engine",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Minimal embeddable example: govern customer-support agent write actions with @osas/policy-engine.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/main.js",
    "demo": "pnpm run build && node dist/main.js"
  },
  "dependencies": {
    "@osas/core": "workspace:*",
    "@osas/policy-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
