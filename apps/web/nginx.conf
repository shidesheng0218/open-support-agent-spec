server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # API proxy: the console talks to the API on same-origin /v1 and /health.
    location /v1/ {
        proxy_pass http://api:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location = /health {
        proxy_pass http://api:3001/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Static web health: proves nginx itself is serving, independent of the API.
    location = /healthz {
        default_type application/json;
        return 200 '{"status":"ok","service":"web"}';
    }

    # SPA fallback for client-side routes (/, /developer, /agent, /platform, /demo).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
